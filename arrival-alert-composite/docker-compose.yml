version: '3.8'

services:
  web:
    build: 
      context: .
      dockerfile: arrival.Dockerfile
    ports:
      - "${PORT:-5000}:5000"
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
    restart: always